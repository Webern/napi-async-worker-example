{
  "name": "napi-async-worker-example",
  "version": "1.0.0",
  "description": "A simple hello world program using the napi.h C++ api and async work.",
  "main": "index.js",
  "scripts": {
    "test": "node-gyp clean && node-gyp configure && node-gyp build && node index.js"
  },
  "author": "Matthew James Briggs",
  "license": "MIT",
  "dependencies": {
    "bindings": "^1.3.0",
    "node-addon-api": "^1.3.0"
  }
}
